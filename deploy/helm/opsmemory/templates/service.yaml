apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsmemory.fullname" . }}-api
  labels:
    {{- include "opsmemory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "opsmemory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
